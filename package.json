{
    "name": "weekly-wallet-monorepo",
    "private": true,
    "devDependencies": {
        "dotenv-cli": "^8.0.0"
    },
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build --workspace=weekly-wallet-frontend"
    }
}